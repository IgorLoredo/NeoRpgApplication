package com.neo.game.character.infrastructure;

/**
 * Legacy/disabled controller kept for repository history.
 * This class is intentionally NOT annotated as a Spring component
 * to avoid bean registration conflicts with the new controller in
 * com.neo.game.character.infrastructure.web.CharacterController.
 */
public class CharacterController {
    // Legacy implementation removed. If you need to restore it,
    // reintroduce the REST annotations and wiring, or keep only the
    // new controller under infrastructure.web.
}
